# WebDrive 安全优化任务

## 已完成

- [x] 修复上传取消接口的本地路径穿越和任意递归删除
- [x] 上传任务绑定登录会话，并校验任务 ID、文件名、分片索引和分片大小
- [x] 增加单文件、临时空间、任务数和任务保留期限限制
- [x] 上传临时目录和文件在文件系统支持时使用 `0700` / `0600` 权限；NAS ACL 不支持 `chmod` 时执行真实写入探测，并在启动时清理过期任务
- [x] HTML、SVG 等主动内容预览强制按纯文本响应，启用 `nosniff`
- [x] 增加 CSP、禁止页面嵌入、Referrer Policy、Permissions Policy 和敏感响应禁用缓存
- [x] 拒绝浏览器跨源写请求，Cookie 改为 `SameSite=Strict`，生产模式默认启用 `Secure`
- [x] 未登录接口不再公开 SMB 地址、域、根目录和本地上传目录
- [x] 增加登录失败限流、全局/单用户会话上限、分享有效期与数量上限
- [x] 服务端增加请求超时和单连接最大请求数
- [x] Docker 默认仅回环发布端口，以非 root 用户和只读根文件系统运行，并移除 Linux capabilities
- [x] Vite 升级到 6.4.3 并移入开发依赖；`npm audit` 当前为 0 漏洞
- [x] 增加服务端安全回归测试

## 需要部署环境配合

- [ ] 在 HTTPS 反向代理配置证书、强制 HTTP 跳转 HTTPS，并确认公网无法直接访问应用端口
- [ ] 将 `AUTH_TOKEN_SECRET` 放入密钥管理系统并制定轮换流程
- [ ] 确认 NAS、WebDrive 与反向代理之间位于可信隔离网络或 VPN
- [ ] 按实际磁盘容量调整上传上限，并配置磁盘空间监控和告警
- [ ] 定期执行依赖审计和容器镜像扫描

## 需要真实 SMB 环境专项验证

- [ ] 评估替换 `@marsaud/smb2`，目标客户端必须支持 SMB3、消息签名和传输加密
- [ ] 使用实际 NAS 覆盖登录、权限拒绝、Range 下载、大文件上传、递归复制/删除和异常断线测试
- [ ] 替换完成后移除 `--openssl-legacy-provider`，并验证所有受支持的 NAS 型号和固件版本
